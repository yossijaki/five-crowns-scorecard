import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'yossijaki.github.io.fivecrowns',
  appName: 'Five Crowns Companion: Party Score',
  webDir: 'dist'
};

export default config;
